# LRA Schemas

This directory will hold canonical schema documentation and migrated or mirrored
schema files for governance, extraction, auditing, and generated artifacts.

Current schema sources remain under `constitution/schema/` and
`constitution/schemas/` until a later migration phase:

- `constitution/schema/file-schema.yaml`
- `constitution/schema/block-registry.yaml`
- `constitution/schema/artifact-matrix.yaml`
- `constitution/schemas/audit-report.json`
