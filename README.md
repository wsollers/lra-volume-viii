# Learning Real Analysis - Volume VIII

Standalone split repository for **Volume VIII: Mathematical Logic and Foundations**.

This repository is a planned-volume stub created during the eight-volume migration. It contains the shared build support files, the `volume-viii/` source tree, and the canonical frontispiece asset `images/hilbert.png`.

Sync back to the monorepo is intentionally deferred. No active `sync-to-monorepo.yml` workflow is installed in this repository yet.
