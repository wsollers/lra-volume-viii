% ---------------------------------------------------------
\section{Limit Properties}

\begin{theorembox}{Theorem (Basic Limit Properties)}
Let $b$, $c$, $L$, and $K$ be real numbers, let $n$ be a positive
integer, and let $f$ and $g$ be functions defined on an open interval
$I$ containing $c$ with
\[
  \lim_{x \to c} f(x) = L \qquad \lim_{x \to c} g(x) = K.
\]
The following limits hold.

\medskip
\begin{tabular}{@{}p{0.22\textwidth}p{0.70\textwidth}@{}}
\textbf{Constants}        & $\displaystyle\lim_{x \to c} b = b$ \\[10pt]
\textbf{Identity}         & $\displaystyle\lim_{x \to c} x = c$ \\[10pt]
\textbf{Sums/Differences} & $\displaystyle\lim_{x \to c}(f(x) \pm g(x)) = L \pm K$ \\[10pt]
\textbf{Scalar Multiples} & $\displaystyle\lim_{x \to c}(b \cdot f(x)) = bL$ \\[10pt]
\textbf{Products}         & $\displaystyle\lim_{x \to c}(f(x) \cdot g(x)) = LK$ \\[10pt]
\textbf{Quotients}        & $\displaystyle\lim_{x \to c}(f(x)/g(x)) = L/K$, \quad $K \neq 0$ \\[10pt]
\textbf{Powers}           & $\displaystyle\lim_{x \to c} f(x)^n = L^n$ \\[10pt]
\textbf{Roots}            & $\displaystyle\lim_{x \to c} \sqrt[n]{f(x)} = \sqrt[n]{L}$ \\[4pt]
                          & (If $n$ is even, require $f(x) \geq 0$ on $I$.) \\[10pt]
\textbf{Compositions}     & If additionally $\displaystyle\lim_{x \to L} g(x) = K$ and $g(L) = K$, \\[6pt]
                          & then $\displaystyle\lim_{x \to c} g(f(x)) = K$. \\
\end{tabular}
\end{theorembox}

\begin{remark*}
For polynomials and rational functions (where the denominator is nonzero),
the limit laws reduce to direct substitution: $\lim_{x \to a} p(x) = p(a)$.
\end{remark*}

\begin{remark*}
The proofs of these properties follow from the $\varepsilon$-$\delta$
definition and are developed in the analysis chapters.
Used together, they reduce the limit of any rational function at a
point where the denominator is nonzero to direct substitution.
\end{remark*}